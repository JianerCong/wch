"""🦜 : What's this?

🐢 : Here is the verifier. It should check whether a file `hi.py` is a valid
pvm contract. In particular, currently we have the following rules:

1. At the top level, the only allowed statements are `def`, `import` and `from ... import ...`

"""
